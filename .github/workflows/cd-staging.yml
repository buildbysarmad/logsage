name: CD — Staging (Disabled)

# Staging environment is not configured
# All code is deployed directly from main to production
# This workflow is disabled and kept for reference only

on:
  workflow_dispatch:

jobs:
  staging-disabled:
    name: Staging Disabled
    runs-on: ubuntu-latest
    steps:
      - name: Staging environment disabled
        run: |
          echo "⚠️  Staging environment is not configured"
          echo "📦 Production deployments happen from main branch only"
          echo "🔀 Merge develop → main to trigger production deployment"
